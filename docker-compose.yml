version: '3.8'

services:
  telegram-proxy:
    build: .
    ports:
      - "5000:5000"  # Web interface
      - "8080:8080"  # Proxy server
    environment:
      - PORT=5000
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
